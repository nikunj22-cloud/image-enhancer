{
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "buildCommand": "npm install -g vite && npx vite build",
  "devCommand": "npx vite",
  "outputDirectory": "dist"
}
